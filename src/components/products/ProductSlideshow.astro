---

interface Props {
  images: string[]
}

const { images } = Astro.props
---

<h1>ProductSlideshow</h1>

<p>
  {images.map((image) => (
    <img src={`${import.meta.env.PUBLIC_URL}/images/products/${image}`} alt="Slideshow" class="h-[350px] object-contain" />
  ))}
</p>